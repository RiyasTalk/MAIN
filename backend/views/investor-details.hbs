<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Details - {{person.personName}}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --google-blue: #1a73e8;
            --background-color: #f8f9fa;
            --surface-color: #ffffff;
            --text-primary: #202124;
            --text-secondary: #5f6368;
            --border-color: #e0e0e0;
        }
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: 'Roboto', 'Segoe UI', sans-serif;
            background-color: var(--background-color);
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 100vh;
            padding: 1rem;
        }
        .details-card {
            background-color: var(--surface-color);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            box-shadow: 0 1px 2px rgba(0,0,0,0.05);
            width: 100%;
            max-width: 700px;
            margin-top: 2rem;
        }
        .card-header {
            background-color: var(--google-blue);
            color: white;
            padding: 1.5rem;
            border-radius: 12px 12px 0 0;
        }
        .card-header h3 {
            font-size: 1.5rem;
            font-weight: 500;
            margin: 0;
        }
        .card-body {
            padding: 1.5rem;
        }
        .details-list {
            list-style: none;
            padding: 0;
            margin: 0 0 2rem 0;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            overflow: hidden;
        }
        .details-list li {
            padding: 1rem 1.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 1rem;
            color: var(--text-primary);
        }
        .details-list li:not(:last-child) {
            border-bottom: 1px solid var(--border-color);
        }
        .details-list strong {
            font-weight: 500;
            color: var(--text-secondary);
        }
        .section-title {
            font-size: 1.25rem;
            font-weight: 500;
            color: var(--text-primary);
            margin-bottom: 1rem;
            padding-bottom: 0.5rem;
            border-bottom: 1px solid var(--border-color);
        }
        .history-table {
            width: 100%;
            border-collapse: collapse;
        }
        .history-table th, .history-table td {
            text-align: left;
            padding: 0.75rem 0;
            border-bottom: 1px solid var(--border-color);
        }
        .history-table th {
            font-size: 0.75rem;
            font-weight: 500;
            color: var(--text-secondary);
            text-transform: uppercase;
        }
        .history-table td {
            font-size: 0.9rem;
        }
        .history-table tr:last-child td {
            border-bottom: none;
        }
        .amount-negative {
            color: #d32f2f;
            font-weight: 500;
        }
        .btn-secondary {
            display: inline-block;
            margin-top: 1.5rem;
            background-color: #f1f3f4;
            color: var(--text-primary);
            border: 1px solid var(--border-color);
            padding: 0.6rem 1.2rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            font-size: 0.9rem;
            transition: background-color 0.2s;
        }
        .btn-secondary:hover {
            background-color: #e8eaed;
        }
        .empty-state {
            text-align: center;
            padding: 2rem;
            color: var(--text-secondary);
        }
    </style>
</head>
<body>
    <div class="details-card">
        <div class="card-header">
            <h3>Details for {{person.personName}}</h3>
        </div>
        <div class="card-body">
            <ul class="details-list">
                <li><strong>Current Investment:</strong> {{formatCurrency person.amount}}</li>
                <li><strong>Initial Investment:</strong> {{formatCurrency person.initialAmount}}</li>
                <li><strong>Mobile Number:</strong> {{person.mobileNumber}}</li>
            </ul>

            <h4 class="section-title">Buyout History</h4>
            
            {{#if buyouts.length}}
                <table class="history-table">
                    <thead>
                        <tr>
                            <th>Date</th>
                            <th style="text-align: right;">Amount</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each buyouts}}
                        <tr>
                            <td>{{formatDate this.buyoutDate}}</td>
                            <td class="amount-negative" style="text-align: right;">-{{formatCurrency this.amount}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            {{else}}
                <div class="empty-state">
                    <p>No buyout history found for this investor.</p>
                </div>
            {{/if}}
            
          
        </div>
    </div>
</body>
</html>